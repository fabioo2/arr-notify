WEBHOOK=https://discord.com/api/webhooks/REPLACE_ME/REPLACE_ME

# Required only by notify-pending.sh (qB-fired). notify-arr.sh reads each
# instance's key from /config/config.xml at runtime since it executes inside
# the arr container, so it doesn't need these.
SONARR_KEY=REPLACE_ME
SONARR_ANIME_KEY=REPLACE_ME
RADARR_KEY=REPLACE_ME
