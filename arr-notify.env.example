WEBHOOK=https://discord.com/api/webhooks/REPLACE_ME/REPLACE_ME

# Optional: how long to wait between grab and import-check, in seconds.
# Defaults to 900 (15 min). Set higher for slow torrents / large releases.
# WAIT_SECONDS=900
